export interface CurrentFilterProps {
  value: string;
  label: string | ReactNode;
}

export interface IClockVar {
  d: string | number;
  dt: string | number;
  mth: string | number;
  y: string | number;
  h: string | number;
  m: string | number;
  s: string | number;
}
